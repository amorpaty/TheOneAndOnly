import React from "react";
import { View } from "react-native";

/**
 * 24.10.20
 * Home 화면 -> CafePictureScreen (카페 사진) 화면 UI 생성
 * @returns 
 */

function CafePictureScreen({navigation, route}) {
    return (
      <View>
      </View>
    );
}
export default CafePictureScreen;
