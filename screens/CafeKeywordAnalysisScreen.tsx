import React from "react";
import { View } from "react-native";

/**
 * 24.10.20
 * Home 화면 -> CafeKeywordAnalysisScreen (카페 키워드 분석) 화면 UI 생성
 * @returns 
 */

function CafeKeywordAnalysisScreen({navigation, route}) {
    return (
      <View>
      </View>
    );
}
export default CafeKeywordAnalysisScreen;
