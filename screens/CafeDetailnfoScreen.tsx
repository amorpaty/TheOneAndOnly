import React from "react";
import { View } from "react-native";

/**
 * 24.10.20
 * Home 화면 -> CafeDetailInfo (카페 정보) 화면 UI 생성
 * @returns 
 */

function CafeDetailInfoScreen({navigation, route}) {
    return (
      <View>
      </View>
    );
}
export default CafeDetailInfoScreen;
