import React from "react";
import { View } from "react-native";

/**
 * 24.10.20
 * Home 화면 -> CafeReviewScreen (카페 리뷰) 화면 UI 생성
 * @returns 
 */

function CafeReviewScreen({navigation, route}) {
    return (
      <View>
      </View>
    );
}
export default CafeReviewScreen;
